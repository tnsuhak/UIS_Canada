# UIS 숙소 QC 공개용 샘플 운영 기준

- `/accommodation-qc.html`의 공개용 리포트는 실제 UIS QC 리포트의 **항목명·구성·관리 흐름**을 살려 보여준다.
- 공개용 전달 형식은 **HTML 리포트**를 기본으로 한다. 한 장짜리 전체 화면 이미지나 스크린샷으로 변환하는 방식을 기본값으로 사용하지 않는다.
- 사진은 웹용으로 최적화된 개별/그룹 WebP 자산을 사용하고, 텍스트는 HTML 텍스트로 유지해 모바일에서도 선명하게 읽히도록 한다.
- 학생·호스트 이름, 얼굴, 연락처 등 직접 식별정보는 공개하지 않는다.
- 국적, 성별, 정확한 개인 생활시간 등 조합 시 학생을 좁힐 수 있는 정보도 공개용 샘플에서는 기본적으로 제거하거나 비식별 처리한다.
- 건강·의료, 과거 수술, 알레르기, 정서·행동의 구체적인 민감 내용은 공개하지 않는다. 필요하면 `건강·생활 상태도 점검`처럼 관리 항목 수준에서만 설명한다.
- 학생/호스트 가족이 식별되는 생활사진은 공개하지 않는다. 불가피하게 생활사진을 사용할 때는 얼굴을 충분히 비식별 처리한다. 식사·숙소 공간처럼 개인 식별이 없는 사진은 사용할 수 있다.
- 영어 원문 항목명은 실제 리포트 느낌을 위해 유지할 수 있으며, 한국 사용자가 바로 이해할 수 있도록 자연스러운 한글 번역/설명을 함께 제공한다.
- 공개용 화면에는 `PUBLIC SAMPLE · 비식별화`임을 명확히 표시한다.
- QC 페이지는 일반 가이드 페이지이므로 별도의 `자료 출처` 박스를 기본 노출하지 않는다.
- QC 리포트 표시를 위해 base64 조각 파일, 빌드 시 이미지 재조립, Edge Function 기반 이미지 대체 등 복잡한 임시 파이프라인을 만들지 않는다. 정적 HTML + 최적화된 정적 이미지 자산을 우선한다.
